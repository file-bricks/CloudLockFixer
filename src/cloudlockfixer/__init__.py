"""CloudLockFixer (CLF-WDAS) — Delayed Action Service für Cloud-Sync-gesperrte
Datei-/Ordner-Operationen (rename/move/delete) via copy+delete-Workaround."""

# Keep this value aligned with [project].version in pyproject.toml.  The
# v1.0.0 tag is the historical initial release, not the current source line.
__version__ = "0.2.2"
__release_state__ = "unreleased"
__release_baseline_tag__ = "v0.2.2"
